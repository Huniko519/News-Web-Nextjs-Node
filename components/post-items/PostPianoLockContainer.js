import React from 'react';

/**
 * Adding container to load lock template from Piano.
 *
 * @returns
 */
const PostPianoLockContainer = () => (
  <div className="piano-lock-container" />
);

export default PostPianoLockContainer;
